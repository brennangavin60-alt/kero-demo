# Kero

Kero is a localStorage-backed demo SaaS for Irish solicitors to create matters, generate correspondence, track workflow stages, and run ad hoc legal research through OpenAI.

## Run

```bash
npm install
npm run dev
```

Add `OPENAI_API_KEY` to `.env.local` to enable ad hoc research.
